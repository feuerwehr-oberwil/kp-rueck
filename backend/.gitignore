__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
.env
*.egg-info/
dist/
build/

# Testing
.pytest_cache/
htmlcov/
.coverage
coverage.xml
*.cover

# Tool caches. Both write a self-ignoring .gitignore inside themselves, which is protection
# that lasts exactly as long as that behaviour does — so name them here too.
.ruff_cache/
.mypy_cache/
