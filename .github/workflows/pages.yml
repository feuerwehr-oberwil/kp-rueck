name: Landingpage

# Publiziert site/ auf GitHub Pages (kp-rueck.ch). Rein statisch – kein Build,
# die Seite im Repo ist die Seite im Netz. site/dist/ wird nicht deployt.
on:
  push:
    branches: [main]
    paths: ['site/**', '.github/workflows/pages.yml']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Ein Deploy aufs Mal; laufende Deploys nicht abwürgen.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: site/ → Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deploy
        uses: actions/deploy-pages@v4
