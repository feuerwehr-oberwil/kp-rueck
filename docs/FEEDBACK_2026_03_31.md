# Feedback Session — 2026-03-31

Testing feedback with all decisions confirmed interactively. Status reconciled 2026-05-29.

---

## ✅ 1. Display Board: Read-Only Incident Detail Modal

**Shipped — modal already existed; enriched 2026-05-28** (commit `73ae39e`). Click any display-board card → modal shows every field from the editor (location, description, contact, notes, Nachbarhilfe/Am-Warten flags + notes, crew with **roles** suffix, vehicles with **driver names** + callsigns + driver-stay, materials, Reko summary). Driver names pulled live via the existing `useVehicleDrivers` hook so the display stays synced with assignments made elsewhere.

---

## ✅ 2. Fahrzeugstatus → Sidebar Sync Delay

**Shipped 2026-05-28** (commits `cc82f56` + earlier D5 work). Extracted `useVehicleDrivers(eventId, enabled)` hook in `frontend/lib/hooks/use-vehicle-drivers.ts` that subscribes to both the `special_function_update` WebSocket event (cross-tab) and the `driver-assignment-changed` window event (same-tab). All 3 consumers (sidepanel, operation-detail-modal, mobile detail sheet) use it.

---

## ✅ 3. "Ressourcen übertragen" Ugly Blue Hover

**Shipped 2026-05-27** (commit `6d6f655`). Both transfer buttons switched from `variant="outline"` to `variant="ghost"` + `border border-border`. Removes the jarring blue hover.

---

## ✅ 4. Critical Emergency Sound

**Shipped 2026-05-28** (commit `019afaf`). The "remove resource-exhaustion sound" half was a no-op — investigation found no such sound in the codebase. The "new-high-priority sound" already existed in operations-context but was unreliable due to browser autoplay. Hardened with:
- One-time `pointerdown` / `keydown` / `touchstart` unlock primes the audio element on first user gesture
- Explicit volume (0.7)
- Retry schedule [0, 500, 1500, 3000] ms
- One-shot `console.warn` if all retries fail while still locked, for debuggability

---

## ✅ 5. Material Resource Blocks (e.g., "Modul 1")

**Verified done 2026-05-28** — fully implemented end-to-end before this session:
- Backend: `MaterialGroup` model + CRUD endpoints (atomic assign-all-available)
- Settings UI: full group management in `MaterialGroupSettings`
- Kanban: `MaterialGroupBlock` renders groups with available/partial/empty status, drag-as-block, expandable children
- Drop on incident → assigns all available children at once

---

## ✅ 6. Special/Consumable Resources (Flatterband etc.)

**Verified done 2026-05-28** — Material schema already has `consumable: bool`. Settings form exposes the switch. Render path shows ∞ icon and excludes consumables from group assignment lists.

---

## ✅ 7. Vehicle Remove Icon: Red on Red

**Shipped earlier** — confirmed in the kanban modal styling pass; `hover:text-destructive` on a `bg-primary` badge replaced with `hover:text-primary-foreground`.

---

## ✅ 8. Display Board & Status Column Headers

**Shipped earlier** — both `display/board` and `display/status` headers now match the main kanban: uppercase title + count in pill badge on the same line, no "Einsätze" suffix.

---

## ✅ 9. Print Format — Additional Fields

**Shipped 2026-05-27** (commit `d2756d0`, audit B10). `crud/print_jobs.build_assignment_payload` consolidated; manual prints (API) and auto-prints (on dispatch) both include: Reko summary, internal notes, Nachbarhilfe note, zu_fuss flag, per-vehicle driver + driver_stay info. Reko-tagged personnel excluded from the crew list in both paths.

---

**All 9 items from this session shipped.**
